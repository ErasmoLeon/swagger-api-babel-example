{
  "name": "mock-factory-api",
  "version": "0.0.1",
  "private": true,
  "description": "New Swagger API Project",
  "keywords": [],
  "author": "",
  "license": "",
  "main": "app.js",
  "scripts": {
    "start-prod": "npm run build && NODE_ENV=production node app.js",
    "start-dev": "nodemon --exec babel-node -- app.js",
    "build": "babel api --out-dir build/api",
    "test": "swagger project test"
  },
  "dependencies": {
    "babel-core": "^6.7.4",
    "babel-preset-es2015": "^6.6.0",
    "restify": "^4.0.2",
    "swagger-restify-mw": "^0.1.0"
  },
  "devDependencies": {
    "nodemon": "^1.9.1",
    "should": "^7.1.0",
    "supertest": "^1.0.0"
  }
}
